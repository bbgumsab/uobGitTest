name: My Workflow File
on:
  push:
    branches: [main]

jobs:
  builds:
    runs-on: Ubuntu- 20.04
    name: My Build
    
    steps:
    - name: Print a greeting
      run:
        echo "Helloo there"
